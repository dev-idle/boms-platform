import brandMark from "./brand-mark.json";

/**
 * Choux tree mark — React + static icons read `brand-mark.json`.
 * After editing JSON: `pnpm generate:brand-icons`
 */

export const BRAND_MARK_VIEW_BOX = brandMark.viewBox;

export const BRAND_MARK_STROKE_WIDTH = brandMark.strokeWidth;

export const BRAND_MARK_PATHS = brandMark.paths;

export const BRAND_MARK_PATH_PARTS = brandMark.pathParts;
