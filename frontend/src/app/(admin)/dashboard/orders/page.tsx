export default function AdminOrdersPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-zinc-900 dark:text-zinc-50">Admin · Orders</h1>
      <p className="mt-2 text-sm text-zinc-600 dark:text-zinc-400">
        Fulfillment workflows should remain auditable on Fiber; the UI renders validated projections only.
      </p>
    </div>
  );
}
