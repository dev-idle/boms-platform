import { cn } from "@/lib/utils";

/** Auth inputs — chrome from `.field-chrome` in globals.css (SSOT tokens). */
export const AUTH_INPUT_CLASS = cn(
  "field-chrome text-form-input",
  "w-full shadow-none ring-0 focus-visible:ring-0",
);
