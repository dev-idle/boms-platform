version: "2"

run:
  tests: true
  timeout: 4m

linters:
  enable:
    # Correctness & cleanliness
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - sqlclosecheck
    - bodyclose
    - noctx
    # Security (OWASP-style static analysis for Go)
    - gosec
  settings:
    gosec:
      excludes:
        - G104 # overlaps with errcheck on non-test code
  exclusions:
    paths:
      - internal/adapter/repository/postgres/sqlcgen

issues:
  max-same-issues: 50
