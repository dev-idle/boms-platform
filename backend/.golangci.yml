version: "2"

run:
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - sqlclosecheck
  exclusions:
    paths:
      - internal/adapter/repository/postgres/sqlcgen

issues:
  max-same-issues: 50
