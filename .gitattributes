# LF everywhere, independent of each developer's core.autocrlf (matches .editorconfig).
# gofmt, Atlas checksums and CI (Linux) all expect LF.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
